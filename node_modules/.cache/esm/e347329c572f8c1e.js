let express,routes;_572‍.w("express",[["default",["express"],function(v){express=v}]]);_572‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);

const app = express();
app.use(express.json());
app.use(express.urlencoded({ extended: false }));
app.use(routes);

const port = process.env.PORT || 3001
app.listen(port, () => {
    console.log(`Server is listening on http://localhost:${port}`)
  })